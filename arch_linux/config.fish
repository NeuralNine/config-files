alias nv nvim
alias ls "ls --color=auto"
alias ll "ls --color=auto -la"
alias vi vim
alias fcf "nv ~/.config/fish/config.fish"
alias ncf "nv ~/.config/nvim/init.vim"

if status is-interactive
    # Commands to run in interactive sessions can go here
end
